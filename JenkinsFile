pipeline {
    stages {
        stage('Submit Stack') {
            steps {
            sh "aws cloudformation create-stack --stack-name MyStack --template-body file://cft_template.json --region 'us-east-2'"
              }
             }
            }
            }
